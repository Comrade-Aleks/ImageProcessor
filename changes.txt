Add image upload
Add Constant checker but keep take picture mode 
take picture mode should remember previous image
auto orientation mode shouldnt be unless main image changes. 

A type of Auto translation possibly with 
a similar character limit and a character limit, so that if nothing is read of if too similar to last, then no translation.

make so that the translation button automatically goes to select region if a picture is not already in file.

add more translator options.

need more settings for the preprocessing, also need to make sure it does not use up the api limit. (or remove the api limit completely if possible)